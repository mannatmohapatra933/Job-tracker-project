# Local Development Settings (PostgreSQL suggested for consistency)
# spring.datasource.url=jdbc:postgresql://localhost:5432/jobtracker
# spring.datasource.username=postgres
# spring.datasource.password=password

# H2 disabled to prevent production conflicts
# spring.datasource.url=jdbc:h2:mem:jobtracker
# spring.h2.console.enabled=true
